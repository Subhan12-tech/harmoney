# `next dev` runs on :3000 while the API runs on :8000, so development needs an
# absolute URL. Production does not: the export is served BY the API, so the
# default empty base gives same-origin requests and no CORS.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
