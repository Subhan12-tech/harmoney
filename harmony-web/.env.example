# Leave UNSET for production. The static export is served by the API itself, so
# an empty base means same-origin requests - no CORS, nothing to configure.
#
# Only set this if you deploy the UI to a DIFFERENT host than the API:
# NEXT_PUBLIC_API_URL=https://harmoney.onrender.com
#
# Local development is handled by .env.development, which is committed.
